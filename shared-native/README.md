# Shared native

This sub-module contains code that is written in native C for the purpose of
deterministic decay calculation across different platforms.
It uses the zig build system to compile the code, no prebuilds.
Zig build take care of toolchain for building.

## Getting started



   
