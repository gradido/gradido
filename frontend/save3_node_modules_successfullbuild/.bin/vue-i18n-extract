#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../vue-i18n-extract/dist-node/index.bin.js" "$@"
else
  exec node  "$basedir/../vue-i18n-extract/dist-node/index.bin.js" "$@"
fi
