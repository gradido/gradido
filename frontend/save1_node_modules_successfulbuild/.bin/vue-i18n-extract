#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../vue-i18n-extract/dist-node/index.bin.js" "$@"
  ret=$?
else 
  node  "$basedir/../vue-i18n-extract/dist-node/index.bin.js" "$@"
  ret=$?
fi
exit $ret
