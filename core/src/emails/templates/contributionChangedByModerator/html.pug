extend ../layout.pug

block content
  h2= t('emails.contributionChangedByModerator.title')
  .text-block
    include ../includes/salutation.pug
    p.human-text= t('emails.contributionChangedByModerator.text', { contributionMemo, senderAlias, contributionMemoUpdated })
  .content
    include ../includes/contributionDetailsCTA.pug
    include ../includes/doNotReply.pug