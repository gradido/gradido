#########################################################################################################
# mariadb server 
#########################################################################################################
FROM mariadb/server:10.3 as mariadb_server

# ENV DOCKER_WORKDIR="/docker-entrypoint-initdb.d"
	
# RUN mkdir -p ${DOCKER_WORKDIR}
# WORKDIR ${DOCKER_WORKDIR}
