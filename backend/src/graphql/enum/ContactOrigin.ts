// AI-GENERATED — not an architecture reference
import { ContactOrigin } from 'shared'
import { registerEnumType } from 'type-graphql'

export { ContactOrigin }

registerEnumType(ContactOrigin, {
  name: 'ContactOrigin',
  description: 'What made two members contacts, where it was not a booking',
})
