enum UserGroup {
  user
  admin
}

type User {
  id: ID!
  firstName: String
  lastName: String
  userName: ID!
  publicKey: String
  privateKey: String
  createdAt: String
  updatedAt: String
  language: String
  disabled: Boolean
  role: UserGroup
  
}
